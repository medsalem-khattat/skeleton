import 'package:flutter/material.dart' show Color;

/// Every project-specific value lives here.
/// When you clone the skeleton for a new app, edit this file first.
class AppConfig {
  static const appName = 'Skeleton';
  static const appVersion = '1.0.0';
  static const seedColor = Color(0xFF3F51B5);

  // Firestore
  static const usersCollection = 'users';
}
